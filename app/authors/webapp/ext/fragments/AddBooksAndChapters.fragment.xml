<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">

    <Dialog
        id="addBooksAndChapters"
        title="Books"
        contentWidth="400px"
        draggable="true"
        resizable="true">

        <content>
            <f:SimpleForm
                id="chapterForm"
                editable="true"
                layout="ResponsiveGridLayout"
                labelSpanXL="4"
                labelSpanL="4"
                labelSpanM="4"
                labelSpanS="12"
                columnsXL="1"
                columnsL="1"
                columnsM="1">

                <f:content>
                    <Label id="books" text="Books" required="true"/>
                    <Input
                        id="chapterNameInput"
                        placeholder="add Book Name"
                        value="{newChapter>/chapterName}"
                        maxLength="100"
                        valueLiveUpdate="true"
                        liveChange=".onChapterNameLiveChange"/>
                </f:content>

            </f:SimpleForm>
        </content>

        <buttons>
            <Button
                id="saveChapterBtn"
                text="Save"
                type="Emphasized"
                enabled="{newChapter>/isValid}"
                press=".onSaveChapter"/>
            <Button
                id="cancelChapterBtn"
                text="Cancel"
                press=".onCancelChapter"/>
        </buttons>

    </Dialog>

</core:FragmentDefinition>